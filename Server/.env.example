# Server Configuration
# Copy this file to .env (development) and .env.production (production)
# Fill in your actual values

# ====================
# Environment
# ====================
# Set to 'production' when deploying
NODE_ENV=development

# ====================
# Server Configuration
# ====================
# Server port (default: 3001)
PORT=3001

# Frontend URL for CORS
# Development: http://localhost:3000
# Production: https://your-domain.com (MUST use HTTPS)
# REQUIRED - will fail to start if missing
CLIENT_URL=http://localhost:3000

# ====================
# Clerk Authentication
# ====================
# Get these from your Clerk Dashboard: https://dashboard.clerk.com
# Development: Use test keys (pk_test_... and sk_test_...)
# Production: Use live keys (pk_live_... and sk_live_...)
# REQUIRED - will fail to start if missing
CLERK_PUBLISHABLE_KEY=pk_test_your-publishable-key
CLERK_SECRET_KEY=sk_test_your-secret-key

# ====================
# Backblaze B2 Storage
# ====================
# Get these from Backblaze B2 Console
# Format for endpoint: s3.<region>.backblazeb2.com
# Example regions: us-west-004, eu-central-003, ap-southeast-001
# REQUIRED - will fail to start if missing
B2_ENDPOINT=s3.region.backblazeb2.com
B2_KEY_ID=your-key-id
B2_APPLICATION_KEY=your-application-key
B2_BUCKET_NAME=your-bucket-name

# ====================
# PostgreSQL Database
# ====================
# Get this from your database provider (Neon, Supabase, etc.)
# Format: postgresql://user:password@host:port/database?sslmode=require
# REQUIRED - will fail to start if missing
# Production: MUST use sslmode=require
DATABASE_URL=postgresql://username:password@host/database?sslmode=require

# ====================
# Optional: Cover Image Generator
# ====================
# Path to Python executable for cover image generation
# If not set, system default python3 will be used
# COVER_PYTHON_PATH=/path/to/python

# ====================
# Production Notes
# ====================
# 1. Set NODE_ENV=production in production deployment
# 2. All URLs MUST use HTTPS in production (except localhost)
# 3. CLIENT_URL is REQUIRED - app will fail to start if missing
# 4. Use Clerk live keys in production (pk_live_... and sk_live_...)
# 5. DATABASE_URL MUST include sslmode=require in production
# 6. Ensure B2_ENDPOINT format is correct: s3.<region>.backblazeb2.com
# 7. Test all environment variables before deployment
# 8. Never commit actual secrets to version control
