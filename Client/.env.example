# Client Configuration
# Copy this file to .env.local (development) and .env.production (production)
# Fill in your actual values

# ====================
# Clerk Authentication
# ====================
# Get these from your Clerk Dashboard: https://dashboard.clerk.com
# Development: Use test keys (pk_test_... and sk_test_...)
# Production: Use live keys (pk_live_... and sk_live_...)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your-publishable-key
CLERK_SECRET_KEY=sk_test_your-secret-key

# Clerk URLs - customize if needed
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_SIGN_IN_FALLBACK_REDIRECT_URL=/library
NEXT_PUBLIC_CLERK_SIGN_UP_FALLBACK_REDIRECT_URL=/library

# ====================
# Application URLs
# ====================
# Your frontend base URL
# Development: http://localhost:3000
# Production: https://your-domain.com (MUST use HTTPS)
APP_BASE_URL=http://localhost:3000

# Backend API URL
# Development: http://localhost:3001
# Production: https://api.your-domain.com (MUST use HTTPS)
# REQUIRED in production - will throw error if missing
NEXT_PUBLIC_API_URL=http://localhost:3001

# ====================
# Image Configuration
# ====================
# Comma-separated list of allowed image hosts for Next.js Image component
# Include your Backblaze B2 domain and any CDN domains
# Format: domain1.com,domain2.com,domain3.com
NEXT_PUBLIC_IMAGE_DOMAINS=s3.region.backblazeb2.com

# ====================
# Production Notes
# ====================
# 1. All URLs in production MUST use HTTPS (except localhost)
# 2. NEXT_PUBLIC_API_URL is REQUIRED in production - app will fail to start if missing
# 3. Use Clerk live keys (pk_live_... and sk_live_...) in production
# 4. Ensure NEXT_PUBLIC_IMAGE_DOMAINS includes your storage domains
# 5. Update APP_BASE_URL to match your deployed frontend URL
