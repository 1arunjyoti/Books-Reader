# Production Environment Variables for BooksReader Frontend
# Copy this to your deployment platform's environment variables section
# DO NOT commit this file to version control

# Generate with: openssl rand -hex 32 (run in terminal)
AUTH0_SECRET=your-production-32-byte-secret-here

# Production application base URL (must be HTTPS)
APP_BASE_URL=https://yourdomain.com

# Auth0 Production Application Settings
AUTH0_DOMAIN=your-production-tenant.auth0.com
AUTH0_CLIENT_ID=your-production-client-id
AUTH0_CLIENT_SECRET=your-production-client-secret

# Auth0 API Audience (must match backend API identifier)
AUTH0_AUDIENCE=https://api.booksreader.com

# Production Backend API URL (must be HTTPS)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# Production image domains (comma-separated)
NEXT_PUBLIC_IMAGE_DOMAINS=s3.eu-central-003.backblazeb2.com,your-cdn-domain.com

# Production Node Environment
NODE_ENV=production